# PostgreSQL connection for local development
DATABASE_URL=postgres://postgres:postgres@localhost:5432/voice_agent_dev

# Optional SSL toggle for hosted/shared environments
PGSSL=false

# Backend HTTP server port
PORT=3001
