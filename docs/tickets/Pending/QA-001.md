# QA-001 — Release evidence

Status: implemented for verification/review; approval is not implied by this packet.
Parent: [foundation checklist](../../FOUNDATION_PROGRESS.md), GitHub #33.
Microtasks: 46–50. Effort varies; these are review units, not equal percentages.
Dependencies: All preceding groups.

## Problem, current behavior and target

Baseline: Baseline checks did not prove a complete authenticated persisted workflow.
Target: the implementation below makes the stated invariant durable and testable while keeping one clinic per deployment, synthetic data and simulated calls.
This packet describes the implemented contract for independent verification. Consult the parent evidence before marking accepted.

## Inspected files and ownership

- `package.json`
- `playwright.config.ts`
- `tests/browser/foundation.spec.ts`
- `tests/migrations.integration.test.cjs`
- `.github/workflows/foundation.yml`
- `docs/FOUNDATION_GUIDE.md`
- `docs/FOUNDATION_PROGRESS.md`

Next.js owns browser cookies and presentation; Node owns identity, permissions and mutations; PostgreSQL owns durable data. No telephony/provider integration, recordings, shared multi-tenant layer or queue infrastructure is added.

## Contract and implementation decisions

CI Node22/PostgreSQL16; local Node25.8.1. Browser ports3100/3101, traces/screenshots /tmp/checkincare-browser-results. All fixture databases must be disposable synthetic data.

## Ordered microtasks and verification checkpoints

### 46 — Separate ordinary, PostgreSQL, fixture and browser commands.

Implement or inspect this behavior in the files above before proceeding. Completion proof: Each fails nonzero on regression; TEST_DATABASE_URL required.
If the checkpoint fails, diagnose the concrete failing assertion before advancing. Keep this logical change independently reviewable and reference #33 and [46/50] in its commit or grouped dependent commit.

### 47 — Run lint/types/tests/migrations/build/browser in hosted CI.

Implement or inspect this behavior in the files above before proceeding. Completion proof: Record hosted run independently from local checks.
If the checkpoint fails, diagnose the concrete failing assertion before advancing. Keep this logical change independently reviewable and reference #33 and [47/50] in its commit or grouped dependent commit.

### 48 — Prove login→intake submission→staff review→PDF and failure paths.

Implement or inspect this behavior in the files above before proceeding. Completion proof: Actual browser forms pass; screenshots desktop/mobile; no page errors.
If the checkpoint fails, diagnose the concrete failing assertion before advancing. Keep this logical change independently reviewable and reference #33 and [48/50] in its commit or grouped dependent commit.

### 49 — Document bootstrap/recovery/migration/rollback/synthetic operation.

Implement or inspect this behavior in the files above before proceeding. Completion proof: Operator can follow guide without shared-token or insecure rollback.
If the checkpoint fails, diagnose the concrete failing assertion before advancing. Keep this logical change independently reviewable and reference #33 and [49/50] in its commit or grouped dependent commit.

### 50 — Attach acceptance evidence and remaining deployment/review gates.

Implement or inspect this behavior in the files above before proceeding. Completion proof: No unobserved deployment, commercial completeness or hosted pass is claimed.
If the checkpoint fails, diagnose the concrete failing assertion before advancing. Keep this logical change independently reviewable and reference #33 and [50/50] in its commit or grouped dependent commit.

## Verification procedure

Prerequisites: locked dependencies installed, migrated disposable PostgreSQL16 database, session auth configuration. Use a separate database from any running application. Integration/browser tests reset fixture tables and must run sequentially against the same fixture database.

Commands (substitute the disposable URL without exposing credentials):

```sh
npm test
npm run lint
npm run typecheck
npm run check:voice-agent
npm run build
TEST_DATABASE_URL=<disposable-url> npm run test:integration
TEST_DATABASE_URL=<disposable-url> npm run test:browser
```

Expected: all relevant assertions pass; invalid credentials/roles are 401/403, invalid payloads400, conflicts409, oversized requests413, throttling429, unavailable persistence503. Public intake continues through submission/review/PDF. Capture exit status and failing test name; a build alone does not establish runtime correctness.

## Failure diagnosis and escalation

PDF after staff review must remain available: reviewed still has a submitted snapshot. Hosted failures are not waived by local success. Deployment requires a configured destination and separate evidence.
Stop if legacy data requires inferred patient identity, a change enables real calls/recordings, or a fix would restore unauthenticated access. Those changes exceed this milestone. Do not erase clinical data to make migration tests green.

## Rollout and rollback

Apply additive migrations before compatible application code in synthetic staging. Retain new tables, audit records and foreign keys. Roll back application code only to a compatible secure version; otherwise disable affected endpoints and fix forward. Never restore shared-token/unauthenticated call access. Down migrations are isolated-test tools, not the deployment rollback plan.

## Completion evidence and review

Record commands, actual outcomes, limitations and hosted CI separately in [FOUNDATION_EVIDENCE](../../FOUNDATION_EVIDENCE.md). Compare the tests above to the corresponding invariant, not just a green process exit. Follow [FOUNDATION_GUIDE](../../FOUNDATION_GUIDE.md) for operator commands and exact shared contracts.
Human review, staging deployment and later commercial release acceptance remain separate gates. Do not move this packet to accepted merely because local implementation is present.
